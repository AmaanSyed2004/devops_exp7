name: CI/CD Pipeline using Github Actions

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm ci

      - name: Run tests with debug and open handles detection
        run: |
          echo "Starting tests..."
          npm test -- --detectOpenHandles
          echo "Tests completed."

      - name: Build Docker image
        run: |
          echo "Building Docker image..."
          docker build -t keyurpatil06/hello-world-app:latest .

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Push Docker image to Docker Hub
        run: |
          echo "Pushing Docker image..."
          docker push keyurpatil06/hello-world-app:latest

      - name: Deploy to localhost
        run: |
          echo "Deploying to localhost..."
          docker pull keyurpatil06/hello-world-app:latest
          
          echo "Stopping old container (if exists)..."
          docker stop hello-world-app-container || true
          
          echo "Removing old container..."
          docker rm hello-world-app-container || true
          
          echo "Starting new container..."
          docker run -d \
            --name hello-world-app-container \
            -p 3000:3000 \
            keyurpatil06/hello-world-app:latest

          echo "Waiting for container to start..."
          sleep 5

          echo "Active containers:"
          docker ps

          echo "Container logs:"
          docker logs hello-world-app-container

      - name: Verify successful deployment
        run: |
          echo "Checking service..."
          curl -f http://localhost:3000 \
            || (docker logs hello-world-app-container && exit 1)

          echo "Service is running successfully! 🎉"
